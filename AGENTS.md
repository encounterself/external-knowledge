# 867 Environmental Study

Use `skills/867-environmental-study/SKILL.md` for this project. Prefer the local
867 resources for explanations and answers. Use the `env867` MCP server for
content search, page reading, public webpage reading, and webpage draft imports.

Webpage imports, formal knowledge-base writes, question generation, Skill
changes, and build tasks require explicit user approval. Never read or write
files outside the project resources and approved application data. Never place
API keys in source, configuration committed to Git, or browser storage.
